apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: deeptech-api
spec:
  rules:
    - host: deeptech.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: deeptech-api
                port:
                  number: 80
